using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerDoorUI : MonoBehaviour
{  /*
    [SerializeField]
    private GameObject _powerCoreRequired;
    [SerializeField]
    private GameObject _restorePower;
    public bool _doorInRange;
    private DoorActivate _doorActive;
    public GameObject playerMain;

    private void Start()
    {
        
    }

    private void Update()
    {
        transform.position = playerMain.transform.position;
      
        if (_doorInRange)
        {
            if (!_doorActive.powerLevelReached)
            {
                _powerCoreRequired.SetActive(true);
                _restorePower.SetActive(false);
            }
            else if (_doorActive.powerLevelReached)
            {
                _powerCoreRequired.SetActive(false);
                _restorePower.SetActive(true);
            }
        }
        else if (!_doorInRange)
        {
            _powerCoreRequired.SetActive(false);
            _restorePower.SetActive(false);
        }
      
    }

    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("DoorTogether"))
        {
            _doorInRange = true;  
        }
    }

    private void OnTriggerExit(Collider other)
    {
        if (other.CompareTag("DoorTogether"))
        {
            _doorInRange = false;
        }
    }
      */
    }
    
